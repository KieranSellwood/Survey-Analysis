Survey Analysis Tool
A command-line program written in C that reads and processes survey data from structured input files, supporting dynamic memory allocation for variable-length datasets.
Build
make
Usage
./survey < input_file
Features

Parses structured survey input files
Dynamically allocates memory for question and response storage
Modular design across multiple source files
